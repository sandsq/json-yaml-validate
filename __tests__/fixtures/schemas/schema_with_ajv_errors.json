{
  "type": "object",
  "required": ["foo", "bar"],
  "properties": {
    "foo": {"type": "integer", "minimum": 2},
    "bar": {"type": "string", "minLength": 4}
  },
  "additionalProperties": false,
  "errorMessage": {
    "properties": {
      "foo": "data.foo should be integer >= 2",
      "bar": "data.bar should be string with length >= 4"
    }
  }
}
